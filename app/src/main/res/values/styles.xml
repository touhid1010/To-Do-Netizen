<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--<item name="android:textColor">@color/textColor</item>-->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />



    <style name="textLabel" parent="TextAppearance.AppCompat">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/textColorHint</item>
        <item name="android:textSize">20sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/floatHighlight</item>
        <item name="colorControlNormal">@color/floatHighlight</item>
        <item name="colorControlActivated">@color/colorControlActivated</item>
        <item name="android:textColorPrimary">@color/textColor</item>

    </style>


    <!--Material button theme-->
    <style name="MyMaterialButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:theme">@style/Theme.MaterialButton</item>
    </style>


    <style name="Theme.MaterialButton" parent="Theme.AppCompat">
        <item name="colorAccent">@color/buttonAccent</item>
        <item name="colorButtonNormal">@color/buttonNormal</item>
    </style>
</resources>
